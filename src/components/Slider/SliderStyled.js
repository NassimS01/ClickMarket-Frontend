import { styled } from "styled-components";

export const ContainerSlider = styled.div`
display: flex;
  justify-content: center;
  align-items: center;
  height: auto;

  & .carousel-container{
    width: 100%;
  }
`;